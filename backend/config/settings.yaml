# AI Receptionist - Application Settings

# Server Configuration
server:
  host: "0.0.0.0"
  port: 8001
  debug: true

# Database
database:
  path: "data/receptionist.db"

# OpenAI Model Configuration
openai:
  model: "gpt-4o-mini"

# CORS Configuration
cors:
  origins:
    - "*"
